import React from 'react'
import PropTypes from 'prop-types'
import "../../CSS/pendingwidthraw.css"

function PendingWithDraw(props) {
    return (
        <div className="pwithdraw" >
            <p>You don't have any pending Withdraw(s)</p>
        </div>
    )
}

PendingWithDraw.propTypes = {

}

export default PendingWithDraw

